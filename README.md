# Веб-форум

Анонимный имиджборд в стиле 4chan, написанный на Go.

![Go](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)
![MySQL](https://img.shields.io/badge/MySQL-8.0+-4479A1?style=flat&logo=mysql&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green.svg)

## 📚 Документация

Подробная документация находится в папке [docs/](./docs/README.md):

- [Обзор проекта](./docs/overview.md)
- [Установка и запуск](./docs/installation.md)
- [Архитектура](./docs/architecture.md)
- [База данных](./docs/database.md)
- [REST API](./docs/api.md)
- [WebSocket API](./docs/websocket.md)
- [Фронтенд](./docs/frontend.md)
- [Конфигурация](./docs/configuration.md)
- [Развёртывание](./docs/deployment.md)

## Возможности

- 📋 **Создание досок** — неограниченное количество тематических досок
- 💬 **Треды с древовидными комментариями** — ответы отображаются лесенкой
- 🔄 **Сортировка тредов** — по бампу, дате создания, количеству ответов
- 📁 **Загрузка медиафайлов** — изображения, видео, аудио (до 100MB)
- 🔍 **Поиск досок** — быстрый поиск по названию и описанию
- 📱 **Адаптивный дизайн** — корректно отображается на мобильных устройствах

## Поддерживаемые форматы файлов

| Тип | Форматы |
|-----|---------|
| Изображения | JPG, JPEG, PNG, GIF, WEBP, SVG |
| Видео | MP4, WEBM, AVI, MOV, MKV |
| Аудио | MP3, WAV, OGG, FLAC, M4A |

## Требования

- Go 1.21+
- MySQL 8.0+

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/webForum.git
cd webForum
```

### 2. Установка зависимостей

```bash
go mod download
```

### 3. Настройка базы данных

Создайте базу данных MySQL:

```sql
CREATE DATABASE webforum CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Настройки базы данных MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=webforum

# Порт сервера
PORT=8080
```

### 5. Запуск

```bash
go run main.go
```

Или соберите и запустите:

```bash
go build -o forum.exe .
./forum.exe
```

Сервер будет доступен по адресу: http://localhost:8080

## Структура проекта

```
webForum/
├── main.go              # Точка входа, маршрутизация
├── go.mod               # Go модуль
├── go.sum               # Контрольные суммы зависимостей
├── .env                 # Конфигурация (не в репозитории)
│
├── database/            # Работа с базой данных
│   ├── database.go      # Подключение к MySQL
│   ├── queries.go       # SQL-запросы
│   └── schema.sql       # Схема базы данных
│
├── handlers/            # HTTP обработчики
│   └── handlers.go      # Все обработчики запросов
│
├── static/              # Статические файлы
│   └── style.css        # Стили
│
├── templates/           # HTML шаблоны
│   ├── index.html       # Главная страница
│   ├── board.html       # Страница доски
│   └── thread.html      # Страница треда
│
└── uploads/             # Загруженные файлы (не в репозитории)
```

## API Endpoints

### Веб-страницы

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Главная страница — список досок |
| GET | `/board/{id}` | Страница доски — список тредов |
| GET | `/board/{id}?sort=bump\|new\|old\|replies` | Сортировка тредов |
| GET | `/thread/{id}` | Страница треда — комментарии |

### REST API v1 (для мобильных приложений)

Все ответы в формате JSON:
```json
{
  "success": true,
  "data": { ... }
}
```

При ошибке:
```json
{
  "success": false,
  "error": "Описание ошибки"
}
```

#### Доски

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/v1/boards` | Список всех досок |
| POST | `/api/v1/boards` | Создать доску |
| GET | `/api/v1/boards/{id}` | Получить доску |
| GET | `/api/v1/boards/{id}/threads?sort=bump` | Получить треды доски |

#### Треды

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/v1/threads/{id}` | Получить тред с постами |
| POST | `/api/v1/threads` | Создать тред |

#### Посты

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/v1/posts` | Создать пост |

#### Медиафайлы

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/v1/upload` | Загрузить медиафайл |

### WebSocket (Live обновления)

| URL | Описание |
|-----|----------|
| `ws://host/ws/thread?thread_id={id}` | Обновления треда |
| `ws://host/ws/board?board_id={id}` | Обновления доски |

---

## Примеры запросов API

### Получить все доски
```bash
curl http://localhost:8080/api/v1/boards
```

Ответ:
```json
{
  "success": true,
  "data": [
    {
      "id": "b",
      "name": "Random",
      "description": "Random topics",
      "thread_count": 5,
      "created_at": "2025-12-06T10:00:00Z"
    }
  ]
}
```

### Создать доску
```bash
curl -X POST http://localhost:8080/api/v1/boards \
  -H "Content-Type: application/json" \
  -d '{"id": "pr", "name": "Программирование", "description": "Pair of programming"}'
```

### Получить треды доски
```bash
curl http://localhost:8080/api/v1/boards/b/threads?sort=bump
```

### Получить тред с постами
```bash
curl http://localhost:8080/api/v1/threads/1
```

Ответ:
```json
{
  "success": true,
  "data": {
    "id": 1,
    "board_id": "b",
    "subject": "Тема треда",
    "post_count": 3,
    "created_at": "2025-12-06T10:00:00Z",
    "bumped_at": "2025-12-06T12:30:00Z",
    "posts": [
      {
        "id": 1,
        "thread_id": 1,
        "parent_id": 0,
        "author": "Аноним",
        "content": "Текст поста",
        "media_path": "/uploads/1_123456.jpg",
        "media_type": "image",
        "created_at": "2025-12-06T10:00:00Z",
        "depth": 0
      }
    ]
  }
}
```

### Создать тред
```bash
curl -X POST http://localhost:8080/api/v1/threads \
  -H "Content-Type: application/json" \
  -d '{
    "board_id": "b",
    "subject": "Новый тред",
    "author": "Аноним",
    "content": "Текст первого поста"
  }'
```

### Создать пост
```bash
curl -X POST http://localhost:8080/api/v1/posts \
  -H "Content-Type: application/json" \
  -d '{
    "thread_id": 1,
    "parent_id": 0,
    "author": "Аноним",
    "content": "Текст ответа"
  }'
```

### Загрузить медиафайл
```bash
curl -X POST http://localhost:8080/api/v1/upload \
  -F "media=@image.jpg"
```

Ответ:
```json
{
  "success": true,
  "data": {
    "path": "/uploads/123456_789.jpg",
    "type": "image"
  }
}
```

Затем используйте `path` при создании поста:
```bash
curl -X POST http://localhost:8080/api/v1/posts \
  -H "Content-Type: application/json" \
  -d '{
    "thread_id": 1,
    "content": "Пост с картинкой",
    "media_path": "/uploads/123456_789.jpg",
    "media_type": "image"
  }'
```

## Скриншоты

### Главная страница
Список всех досок с поиском и возможностью создания новой доски.

### Страница доски
Список тредов с сортировкой и формой создания нового треда.

### Страница треда
Древовидные комментарии (лесенка) с возможностью ответа на конкретный пост.

## Технологии

- **Backend:** Go (net/http)
- **Database:** MySQL
- **Frontend:** HTML, CSS, JavaScript (vanilla)
- **Шаблонизатор:** html/template

## Лицензия

MIT License


